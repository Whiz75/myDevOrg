import { LightningElement, api } from 'lwc';

export default class ConfirmationAndUpdateScreen extends LightningElement {
    @api selectedAccounts = []; // Receives the selected accounts from the parent

    // Additional logic to handle the selected accounts can go here
}
